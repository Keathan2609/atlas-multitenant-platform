# Hoist nothing by default — surfaces missing dependency declarations early,
# which is what we want in a monorepo with strict package boundaries.
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
